# IDQ-002 fixture override on the isolated IDQ stack; never mounts live credentials.
services:
  backend:
    command: python -m uvicorn tests.e2e.portfolio_risk_app:app --host 0.0.0.0 --port 8000
  browser:
    command: npm run test:e2e:portfolio-risk
