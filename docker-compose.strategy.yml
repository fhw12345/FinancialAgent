# Confirmed research-contract acceptance on the isolated IDQ stack, never live policy data.
services:
  backend:
    environment:
      FINNHUB_API_KEY: dummy
    command: python -m uvicorn tests.e2e.strategy_app:app --host 0.0.0.0 --port 8000
  browser:
    command: npm run test:e2e:strategy
