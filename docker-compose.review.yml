# IDQ-001-B acceptance only. No live policies or credentials are reused.
services:
  backend:
    environment:
      FINNHUB_API_KEY: dummy
    command: python -m uvicorn tests.e2e.review_app:app --host 0.0.0.0 --port 8000
  browser:
    command: npm run test:e2e:review
