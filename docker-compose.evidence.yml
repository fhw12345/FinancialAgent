# Evidence acceptance on the isolated IDQ stack. No live credentials or policies.
services:
  backend:
    environment:
      FINNHUB_API_KEY: dummy
    command: python -m uvicorn tests.e2e.evidence_app:app --host 0.0.0.0 --port 8000
  browser:
    command: npm run test:e2e:evidence
